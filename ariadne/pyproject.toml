[project]
name = "ariadne"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { email = "github@michaeljanssen.nl" }
]
requires-python = ">=3.12"
dependencies = [
    "click>=8.3.0",
]

[project.scripts]
ariadne = "ariadne.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
